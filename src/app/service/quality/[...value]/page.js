export default function Value({params}) {
    return (
      <div>
        <h2>Product value is {params.value [0]}</h2>
        <h2>Product value is {params.value [1]}</h2>
        <h2>Product value is {params.value [2]}</h2>
      </div>
    )
  }